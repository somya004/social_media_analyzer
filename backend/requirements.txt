# Web
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0

# AI / LangChain
langchain==0.2.5
langchain-google-genai==1.0.6
google-generativeai==0.5.4

# Embeddings
sentence-transformers==3.0.0
# torch is a transitive dep of sentence-transformers; letting pip resolve the
# right variant (CPU vs CUDA) avoids forcing a 2 GB CUDA wheel on CPU machines.
# Pin only if you need a specific CUDA build:
#   torch==2.3.1+cu121 --index-url https://download.pytorch.org/whl/cu121

# Vector DB
chromadb==0.5.0

# YouTube extraction
youtube-transcript-api==0.6.2
yt-dlp==2024.5.27

# Instagram extraction
apify-client==1.7.1
